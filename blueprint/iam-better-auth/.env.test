# Test environment variables for iam-better-auth
# These variables are loaded during test execution

# Database configuration (optional - tests use testcontainers by default)
DATABASE_TYPE=postgres

# Password encryption (provided by customEnvVars in test setup)
PASSWORD_ENCRYPTION_SECRET=test-password-secret-key-for-testing
PASSWORD_ENCRYPTION_SECRET_PATH=../private.pem
CORS_ORIGINS=http://localhost:3000
