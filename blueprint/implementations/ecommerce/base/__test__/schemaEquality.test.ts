import { describe, it } from 'vitest';

// Per-entity zod/typebox parity tests are added incrementally as each
// entity's PR lands — none yet.
describe('schemaEquality', () => {
  it('has no entities registered yet', () => {
    // placeholder — filled in as entities land
  });
});
